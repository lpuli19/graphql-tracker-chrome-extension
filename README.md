# graphql-tracker-chrome-extension
Chrome extension that intercepts and logs all GraphQL network requests in the browser, with request/response details to help debug APIs and understand client–server interactions.
